// /v6: clone of the /new homepage. Re-exports the same component so both
// stay in sync. If /v6 needs to diverge, replace this with a full copy
// and iterate on it independently.
export { default } from "@/pages/home-new";
