// /v5: clone of the /new homepage. Re-exports the same component so both
// routes stay in sync — split this into a full file copy if /v5 ever needs
// to diverge from /new.
export { default } from "@/pages/home-new";
